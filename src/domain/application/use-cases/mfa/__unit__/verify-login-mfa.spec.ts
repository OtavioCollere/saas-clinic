import { describe } from "vitest";

describe('SetupMfaUseCase Unit Tests', () => {

  it('should be able to setup MFA')

  it('should not be able to setup MFA if user does not exist')

  it('should not be able to setup MFA if user already has MFA')
  
  it('should not be able to setup MFA if invalid totp code')
})